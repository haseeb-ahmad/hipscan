%h1 Change account settings
= form_for @account, :url => { :action => 'update' }, :html => { :method => :put } do |f|
  = f.error_messages
  = f.text_field :name
  = submit_or_cancel :action => 'show'
